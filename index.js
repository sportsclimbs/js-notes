console.log('Hellow Worldy');

let name = 'Cyamodus'; // String literal
let surname = 'Smithy'
let age = 30;         // Number literal
let isApproved = 'true';    // Boolean literal
let firstName = 'undefined';  // not used very often
let selectedColor = null;     // When the user hasn't chosen a color

// In the console use typeof to see what these variables are eg type in typeof selecteedColor


console.log(name);

const interestRate = '0.3'; // a contant
console.log(interestRate);

let person = {
    name: 'Cyamodus',
    age: 147,
    hairColor: 'red'
}

console.log(person);

console.log(person.age);


// My first function...

function greet(name) {
    console.log('Hello ' + name);
}

greet('John');
greet('Cyamodus');
